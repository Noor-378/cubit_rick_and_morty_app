import 'package:flutter/material.dart';

class CharectersScreen extends StatelessWidget {
  const CharectersScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar());
  }
}
